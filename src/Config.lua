-- Shared plugin config. Keep BaseUrl's port in sync with ROBLOX_MCP_PORT.
return {
	BaseUrl = "http://127.0.0.1:3070",
	-- Seconds to back off when the MCP server is unreachable.
	ReconnectDelay = 2,
}
